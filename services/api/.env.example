# =============================================================================
# Maestro API — Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in real values.
# .env.local is gitignored and overrides .env values.

# Database — local Docker Postgres (via docker compose up -d)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=maestro
DB_NAME=maestro

# Dev mode — bypasses JWT auth. Use your real Supabase user ID
# so data associations (projects, queries) work correctly.
DEV_USER_ID=your-supabase-user-uuid-here

# Supabase (keep production values for Storage access)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key
SUPABASE_JWT_SECRET=your-jwt-secret

# AI Services
GEMINI_API_KEY=your-gemini-key
VOYAGE_API_KEY=your-voyage-key
# ANTHROPIC_API_KEY=your-anthropic-key
# OPENROUTER_API_KEY=your-openrouter-key

# CORS — frontend URL
FRONTEND_URL=http://localhost:3000
